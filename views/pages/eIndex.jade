extend ../layout

block content
  .container
    .row
      .col-md-6
        button.btn.btn-primary(type="button",id="upload") #{movie.country}
      .col-md-6
        button.btn.btn-primary(type="button",id="download") #{movie.title}
